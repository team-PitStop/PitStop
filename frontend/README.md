# PitStop Frontend

React 19 + Vite. Run `npm install` then `npm start` — the dev server serves on http://localhost:5173.

The backend API must be running separately on port 8080 (`cd backend && ./mvnw spring-boot:run`).

See the [root README](../README.md) for full setup, configuration, and deployment docs.
